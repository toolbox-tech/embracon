steps:
  - name: Cache dependencies
    uses: actions/cache@v3
    with:
      path: |
        ~/.cache/pip
        ~/.npm
        **/node_modules
        **/vendor/bundle
      key: ${{ runner.os }}-deps-${{ hashFiles('**/lockfiles') }}
      restore-keys: |
        ${{ runner.os }}-deps-